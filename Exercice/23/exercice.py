import os
import utils

scans = utils.read_file(os.getcwd() + "\\input.txt")

#full recursion avec une liste qui retient les positions et qui empèche de la refaire 2 fois
if __name__ == "__main__":
    pass
